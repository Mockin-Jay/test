/**
 * OverlayScreen - Stripped-down canvas-only view for the OBS overlay window.
 * No UI chrome, no background. Only the PixiJS avatar canvas.
 * Will be implemented in Phase 6 (OBS Integration).
 */
export default function OverlayScreen() {
  return (
    <div className="w-full h-full">
      {/* AvatarCanvas will go here in Phase 6 */}
    </div>
  );
}
